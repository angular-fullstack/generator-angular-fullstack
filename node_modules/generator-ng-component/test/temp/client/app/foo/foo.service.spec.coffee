'use strict'

describe 'Service: foo', ->

  # load the service's module
  beforeEach module('tempApp')

  # instantiate service
  foo = undefined
  beforeEach inject((_foo_) ->
    foo = _foo_
  )
  it 'should do something', ->
    expect(!!foo).toBe true
